const request = require("supertest");

describe("jokes-router", () => {
  describe("GET /api/jokes", () => {
    it.todo("rejects unauthorized users");
    it.todo("returns a list of jokes");
  });
});
